[project]
name = "linkedin-scan"
version = "0.1.0"
description = "Monetize360 LinkedIn scan backend and report API."
requires-python = ">=3.11"
dependencies = [
    "fastapi>=0.111.0",
    "uvicorn[standard]>=0.30.0",
    "pydantic>=2.7.0",
]

[tool.uv]
package = false
